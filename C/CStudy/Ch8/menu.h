#pragma once

#include <stdio.h>
#include <stdbool.h>
#include <Windows.h>  //cls 명령어를 치기위해
#include <conio.h> //_getch()

/*
* 함수 (Function)
* 
* 변수의 선언
* 데이터타입 변수이름;
* int variableName;
* 
* 함수의 선언
* (데이터타입) 함수이름 ();
* 리턴타입
*/

/*
* 호출
* 변수의 사용법 : 변수이름을 가져와서 사용한다.
* 함수의 사용법 : 함수이름을 가져와서 사용한다.
* 뭐가 변수인지 함수인지 구분방법 : 함수 뒤쪽에는 ()
*/

/*
* 헤더파일은 함수를 선언하여 사용한다.
*/


void SelectMenu(); // 데이터타입 함수이름 () ;